# ============================================
# VAKMK AI — Backend dependencies
# ============================================

# --- Web framework ---
fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.9

# --- Database ---
sqlalchemy==2.0.34
psycopg2-binary==2.9.9
alembic==1.13.2

# --- Auth ---
passlib[bcrypt]==1.7.4
python-jose[cryptography]==3.3.0
bcrypt==4.0.1

# --- Settings & validation ---
pydantic==2.9.2
pydantic-settings==2.5.2
email-validator==2.2.0

# --- AI / RAG ---
# Default provider: Google Gemini (free tier, no credit card required)
google-generativeai==0.8.3
# Optional: OpenAI (paid). Kept so users can switch via LLM_PROVIDER=openai
openai==1.51.0
langchain==0.3.0
langchain-openai==0.2.0
langchain-community==0.3.0
langchain-text-splitters==0.3.0
chromadb==0.5.5
tiktoken==0.7.0

# --- Document parsing ---
pypdf==4.3.1
python-docx==1.1.2

# --- Utilities ---
python-dotenv==1.0.1
httpx==0.27.2
tenacity>=8.2.3,<9.0.0
